---
title: Fazer backup de um banco
description: Crie e restaure backups de banco de dados com seguranÃ§a.
---

Backups protegem dados antes de migraÃ§Ãµes, mudanÃ§as arriscadas ou manutenÃ§Ã£o em produÃ§Ã£o.

## Objetivo

Criar um backup manual de um banco de dados.

## Quando usar

FaÃ§a backup antes de mudanÃ§as de schema, imports, rotaÃ§Ã£o de senha ou manutenÃ§Ã£o manual.

## Exemplo

```bash
curl -X POST https://api.hyzecloud.com/api/databases/db_001/backups \
  -H "Authorization: Bearer hyze_sua_chave"
```

## Restaurar

```bash
curl -X POST https://api.hyzecloud.com/api/databases/db_001/restore \
  -H "Authorization: Bearer hyze_sua_chave" \
  -H "Content-Type: application/json" \
  -d '{ "backupId": "backup_db_001" }'
```

<Warning>
  RestauraÃ§Ãµes podem substituir dados ativos. Confirme o banco de destino antes de executar em produÃ§Ã£o.
</Warning>

## PrÃ³ximos passos

<CardGroup cols={2}>
  <Card title="API de bancos" icon="database" href="/api-reference/databases">
    Veja endpoints de backup e restauraÃ§Ã£o.
  </Card>
  <Card title="Provisionar um banco" icon="plus" href="/pt/guides/provision-a-database">
    Crie um novo banco primeiro.
  </Card>
</CardGroup>
