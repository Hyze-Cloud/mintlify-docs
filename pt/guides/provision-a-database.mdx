---
title: Provisionar um banco de dados
description: Crie um banco de dados gerenciado e conecte-o ao seu app.
---

A Hyze Cloud pode provisionar PostgreSQL, MySQL, MongoDB e Redis atravÃ©s da mesma API de ciclo de vida de bancos.

## Objetivo

Criar um banco de dados e conectÃ¡-lo a um app.

## Quando usar

Use quando seu app precisar de armazenamento persistente, cache, filas ou dados documentais.

## Criar um banco de dados

<Steps>
  <Step title="Escolha um engine">
    Escolha `postgresql`, `mysql`, `mongodb` ou `redis`.
  </Step>
  <Step title="Crie a instÃ¢ncia">
    ```bash
    curl -X POST https://api.hyzecloud.com/api/databases/ \
      -H "Authorization: Bearer hyze_sua_chave" \
      -H "Content-Type: application/json" \
      -d '{
        "name": "prod-postgres",
        "engine": "postgresql",
        "memoryMB": 1024,
        "storageGB": 20
      }'
    ```
  </Step>
  <Step title="Guarde a string de conexÃ£o">
    Copie o `connectionString` retornado e salve como variÃ¡vel de ambiente do seu app.
  </Step>
</Steps>

<Callout type="warning">
  Trate strings de conexÃ£o como segredos. NÃ£o as commite no controle de versÃ£o nem cole em logs.
</Callout>

## Adicionar a string de conexÃ£o a um app

```bash
curl -X PUT https://api.hyzecloud.com/api/apps/app_001/env \
  -H "Authorization: Bearer hyze_sua_chave" \
  -H "Content-Type: application/json" \
  -d '{
    "envVars": {
      "DATABASE_URL": "postgresql://user:pass@host:5432/db"
    }
  }'
```

## Como funciona

A Hyze Cloud provisiona o banco, retorna uma string de conexÃ£o e permite que vocÃª passe esse valor para seu app atravÃ©s de variÃ¡veis de ambiente.

## Operar o banco de dados

| Tarefa | Endpoint |
| --- | --- |
| Ver estatÃ­sticas | [`GET /api/databases/:databaseId/stats`](/api-reference/databases#get-stats) |
| Ler logs | [`GET /api/databases/:databaseId/logs`](/api-reference/databases#get-logs) |
| Rotacionar senha | [`POST /api/databases/:databaseId/rotate-password`](/api-reference/databases#rotate-password) |
| Criar backup | [`POST /api/databases/:databaseId/backups`](/api-reference/databases#create-backup) |
| Restaurar backup | [`POST /api/databases/:databaseId/restore`](/api-reference/databases#restore-backup) |

## Endpoints relacionados

<CardGroup cols={2}>
  <Card title="ReferÃªncia de bancos" icon="database" href="/api-reference/databases">
    Crie, inspecione, opere, faÃ§a backup e restaure bancos de dados.
  </Card>
  <Card title="Ambiente do app" icon="key" href="/api-reference/apps#set-environment-variables">
    Armazene segredos de runtime para seu app.
  </Card>
</CardGroup>
