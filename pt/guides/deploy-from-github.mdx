---
title: Deploy pelo GitHub
description: FaÃ§a deploy de um app a partir de um repositÃ³rio GitHub conectado.
---

Use deploys por GitHub quando seu cÃ³digo-fonte jÃ¡ estiver em um repositÃ³rio.

## Objetivo

Criar um app a partir de um repositÃ³rio e opcionalmente ativar auto deploys.

## Quando usar

Use este fluxo para apps de produÃ§Ã£o, workflows em equipe e deploys por branch.

## Exemplo

```bash
curl -X POST https://api.hyzecloud.com/api/apps/deploy-from-repo \
  -H "Authorization: Bearer hyze_sua_chave" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "api",
    "runtime": "node",
    "memoryMB": 512,
    "startupCommand": "npm start",
    "exposePort": 3000,
    "autoDeploy": true,
    "repository": {
      "owner": "hyzecloud",
      "name": "api",
      "branch": "main"
    }
  }'
```

## Como funciona

A Hyze Cloud baixa a branch selecionada, faz o build do app e inicia com seu `startupCommand`.

<Tip>
  Ative `autoDeploy` para que pushes na branch disparem novos deploys automaticamente.
</Tip>

## PrÃ³ximos passos

<CardGroup cols={2}>
  <Card title="Ver logs" icon="terminal" href="/pt/guides/view-logs">
    Inspecione a saÃ­da de inicializaÃ§Ã£o e runtime.
  </Card>
  <Card title="API de Apps" icon="book-open" href="/api-reference/apps">
    Veja todos os endpoints de apps.
  </Card>
</CardGroup>
