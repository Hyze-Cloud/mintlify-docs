---
title: App com banco de dados
description: Crie um banco de dados e passe sua string de conexÃ£o para um app.
---

Use este fluxo para APIs que precisam de armazenamento persistente.

## Objetivo

Provisionar PostgreSQL e anexÃ¡-lo a um app como `DATABASE_URL`.

## Criar o banco de dados

```bash
curl -X POST https://api.hyzecloud.com/api/databases/ \
  -H "Authorization: Bearer hyze_sua_chave" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "prod-postgres",
    "engine": "postgresql",
    "memoryMB": 1024,
    "storageGB": 20
  }'
```

## Definir a variÃ¡vel do app

```bash
curl -X PUT https://api.hyzecloud.com/api/apps/app_001/env \
  -H "Authorization: Bearer hyze_sua_chave" \
  -H "Content-Type: application/json" \
  -d '{
    "envVars": {
      "DATABASE_URL": "postgresql://user:pass@host:5432/db"
    }
  }'
```

## PrÃ³ximos passos

<CardGroup cols={2}>
  <Card title="Provisionar um banco" icon="database" href="/pt/guides/provision-a-database">
    Aprenda o ciclo de vida de bancos de dados.
  </Card>
  <Card title="Gerenciar segredos" icon="key" href="/pt/guides/manage-secrets">
    Mantenha credenciais seguras.
  </Card>
</CardGroup>
