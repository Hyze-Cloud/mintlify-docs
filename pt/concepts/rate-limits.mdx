---
title: Rate limits
description: Limites de requisiÃ§Ã£o no proxy pÃºblico e na API da Hyze Cloud. Entenda 429, defaults por IP e host, e a diferenÃ§a de pause por abuse.
keywords: ["rate limit", "429", "proxy", "Hyze Cloud", "req/s", "Retry-After", "abuse"]
"og:title": "Rate limits â€” Hyze Cloud"
"og:description": "80 req/s por IP e 400 req/s por host no proxy pÃºblico. Como funciona o 429 e o que nÃ£o Ã© pause de app."
---

A Hyze Cloud aplica **rate limits** para proteger apps publicados e a prÃ³pria API. Quando o limite Ã© atingido, a resposta Ã© **`429 Too Many Requests`** â€” o site continua no ar, sÃ³ freia o excesso de trÃ¡fego.

## Dois lugares diferentes

| Onde | O que limita | Quando vocÃª vÃª |
| --- | --- | --- |
| **Proxy pÃºblico** | TrÃ¡fego HTTP no subdomÃ­nio/domÃ­nio do app | Visitantes, bots, load tests no site |
| **API Hyze** | Chamadas a `api.hyzecloud.com` | Deploy, logs, keys, automaÃ§Ã£o |

Esta pÃ¡gina foca no **proxy pÃºblico** (site/app exposto). A API tambÃ©m pode retornar `429` em alguns endpoints â€” veja a [introduÃ§Ã£o da API](/api-reference/introduction).

## Proxy pÃºblico (apps com website)

Todo request a um app publicado passa pelo edge da Hyze:

```text
Cliente â†’ proxy Hyze â†’ seu container
```

O rate limit roda **no proxy**, antes de chegar no seu processo.

### Limites padrÃ£o

Janela fixa de **1 segundo** (por processo de proxy):

| DimensÃ£o | Default | Significado |
| --- | --- | --- |
| **Por IP + host** | **80 req/s** | Um mesmo cliente (IP) no seu subdomÃ­nio |
| **Por host** | **400 req/s** | Todos os clientes somados naquele subdomÃ­nio |

Exemplos:

- `meu-app.hyzecloud.app` recebe 50 req/s de um IP â†’ ok  
- O mesmo IP manda **100 req/s** â†’ parte vira **429**  
- VÃ¡rios IPs somam **500 req/s** no mesmo host â†’ o teto por host corta o excesso  

### Resposta 429

Quando o limite estoura:

- **Status:** `429 Too Many Requests`
- **Header:** `Retry-After` (segundos para tentar de novo)
- **Headers extras (quando aplicÃ¡vel):** `X-RateLimit-Limit`, `X-RateLimit-Remaining`
- **HTML:** pÃ¡gina simples â€œMuitas requisiÃ§Ãµesâ€ (visitantes)
- **JSON:** corpo com `error: "RATE_LIMITED"` (clientes API/fetch)

Isso Ã© diferente de **503** (app parado, subindo ou sem resposta).

| CÃ³digo | Significado comum na Hyze |
| --- | --- |
| **429** | TrÃ¡fego demais â€” espere e tente de novo |
| **503** | App indisponÃ­vel / ainda iniciando / falha de rota |
| **404** | SubdomÃ­nio ou domÃ­nio sem app publicado |

### Boas prÃ¡ticas

- FaÃ§a o app escutar em **`0.0.0.0`** e na porta configurada como **expose**
- Use **cache** e CDN para assets estÃ¡ticos quando possÃ­vel
- Evite polling agressivo de frontends (prefira intervalos razoÃ¡veis ou websocket)
- Load tests internos contam como trÃ¡fego real â€” o proxy e as proteÃ§Ãµes de abuse reagem

### Rate limit vs abuse (pause)

| | Rate limit (proxy) | Abuse / pause |
| --- | --- | --- |
| **Age em** | milissegundos | ~heartbeat (dezenas de segundos) |
| **Resposta** | 429 no edge | App pode ser **pausado** |
| **Foco** | quem **bate** no site | app que **gera** abuso de saÃ­da (egress) |
| **Objetivo** | nÃ£o derrubar o site com flood | nÃ£o usar a Hyze como origem de ataque |

Os dois se complementam. Rate limit protege o app que **recebe** trÃ¡fego; pause por abuse atua em apps que **disparam** conexÃµes em massa de dentro da plataforma.

## API da Hyze Cloud

Alguns endpoints da API (`api.hyzecloud.com`) tambÃ©m tÃªm rate limit e retornam `429` com `Retry-After`.

RecomendaÃ§Ãµes:

- Reutilize a mesma API key com cuidado em loops paralelos
- Respeite `Retry-After` em vez de retentar na hora
- Prefira webhooks/eventos a polling agressivo quando existir a opÃ§Ã£o

Detalhes por endpoint podem variar â€” consulte a [referÃªncia da API](/api-reference/introduction).

## FAQ rÃ¡pido

### Meu site legÃ­timo pode levar 429?

TrÃ¡fego humano normal e APIs bem comportadas raramente passam de dezenas de req/s por IP. Spikes extremos (load test, bug de retry, scrape) sÃ£o os casos tÃ­picos de 429.

### Posso desligar o rate limit do meu app?

Os limites do proxy sÃ£o de **plataforma** (proteÃ§Ã£o multi-tenant). NÃ£o hÃ¡ toggle por app na documentaÃ§Ã£o pÃºblica. Planos futuros podem oferecer tetos maiores.

### 429 significa que meu app foi pausado?

NÃ£o. Pause por abuse Ã© outra proteÃ§Ã£o. Com 429 o app continua rodando; sÃ³ o excesso de requests Ã© recusado no edge.

## PrÃ³ximos passos

<CardGroup cols={2}>
  <Card title="Apps" icon="cube" href="/pt/concepts/apps">
    O que um app inclui e como configurar.
  </Card>
  <Card title="Deploy de um app" icon="rocket" href="/pt/guides/deploy-an-app">
    Publique e exponha a porta correta.
  </Card>
  <Card title="Ver logs" icon="scroll" href="/pt/guides/view-logs">
    Depure o processo do app.
  </Card>
  <Card title="FAQ" icon="circle-question" href="/pt/faq">
    Outras perguntas comuns.
  </Card>
</CardGroup>
